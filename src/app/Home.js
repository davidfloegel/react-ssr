import React from 'react';
import { Helmet } from 'react-helmet';

const Home = () => (
  <div>
    <Helmet>
      <title>Home</title>
    </Helmet>
    <b>This is Home :) or is it...? :)</b>
  </div>
);

export default Home;
