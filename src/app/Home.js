import React from 'react';
import { Helmet } from 'react-helmet';

const Home = () => (
  <div>
    <Helmet>
      <title>Home</title>
    </Helmet>
    <b>This is Home</b>
  </div>
);

export default Home;
